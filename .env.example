# Port the production server listens on (npm start). Defaults to 3000.
PORT=3000

# Optional: absolute path to the SQLite database file.
# Defaults to <repo-root>/data/diy-shed.db when unset.
# DIYSHED_DB=/absolute/path/to/diy-shed.db

# Research (M2): headless auth for the Claude CLI. Generate a long-lived token
# with `claude setup-token` and put it here (kept out of git via .env) — the
# server passes it to the CLI process it spawns, so research runs without an
# interactive login. Without valid auth, research fails as "Not logged in".
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...

# Research (M2): path to the Claude Code CLI binary. Resolution order is: this
# override → `claude` on PATH → the newest binary bundled with the Claude desktop
# app (auto-discovered, e.g. %APPDATA%\Claude\claude-code\<version>\claude.exe).
# Set this only to force a specific binary:
# DIYSHED_CLAUDE_BIN=C:\Users\you\AppData\Roaming\Claude\claude-code\<version>\claude.exe

# Research (M2): how long a single Claude CLI research run may take before it is
# killed. Milliseconds; defaults to 300000 (5 min).
# DIYSHED_RESEARCH_TIMEOUT_MS=300000

# Research (M2): max concurrent research jobs in the in-process queue.
# Defaults to 1, capped at 2.
# DIYSHED_RESEARCH_CONCURRENCY=1
